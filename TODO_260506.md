# TODO_260506 — Closing the Gap to Vision (Variant 1)

Comparing the current app screenshot against `Vision/Overview_Architecture Variant1.png`.

---

## 1. App Header / Top Navigation

**Current:** Four labelled button-group panels (WORKSPACE / PROFILES / DEVTOOLS / APP) that are wide and noisy.

**Vision:** A single slim top bar containing:
- `codomon` logo + icon on the far left
- Workspace name as a styled dropdown (e.g. "AnimalZoo ▾") next to the logo
- Five primary navigation tabs centred: **Design · Scan · Monitor · Graph · Docs**
- Far right: green `● Running` pill + red `■ Stop` button + `⚙` settings icon

### Tasks
- [ ] Add a `codomon` logo/icon asset and display it as a small image+wordmark in the top-left corner of `MainWindow.axaml`.
- [ ] Replace the PROFILES ComboBox + New/Rename/Duplicate/Delete buttons with a single workspace-name dropdown that shows the active workspace name and a `▾` chevron.
- [ ] Replace the WORKSPACE / DEVTOOLS / APP button groups with five centred tab-style navigation buttons: Design, Scan, Monitor, Graph, Docs — switching the main content area accordingly.
- [ ] Add a `● Running / ■ Stop` runtime-status indicator to the top-right (wire to `LiveMonitorViewModel` running state).
- [ ] Move the **Settings** button to the far-right corner as a gear icon only (no label).
- [ ] Remove the APP / PROFILES / WORKSPACE group labels and borders; flatten the toolbar height.

---

## 2. Left Sidebar — Full Navigation Panel

**Current:** A single "Architecture" label above a bare `TreeView` that is only visible when a workspace is open.

**Vision:** A rich multi-section sidebar always visible once a workspace is loaded:

```
WORKSPACE
  📁 AnimalZoo
     AnimalZoo.sln  (C:\Dev\AnimalZoo)

ANALYSIS
  📋 Scan Summary
  📄 Source Files        1283
  📝 Markdown Docs       1283
  🔷 AST Graph
  📍 Log Points          312

ARCHITECTURE MAP  [+]
  ● Presentation Layer
    · ZooConsoleUI
    · WebControllers
  ● Application Layer
    · AnimalManager  ← (selected)
    · FeedingService
    …
  ● Domain Layer …
  ● Infrastructure Layer …

─────────────────────
[D] developer
    Local Workspace  ⚙
```

### Tasks
- [ ] Add a **WORKSPACE** section at the top of the sidebar showing the workspace folder name and the solution file path beneath it.
- [ ] Add an **ANALYSIS** section with clickable rows: Scan Summary, Source Files (with count badge), Markdown Docs (with count badge), AST Graph, Log Points (with count badge). Counts are read from `MainViewModel`.
- [ ] Rename the existing "Architecture" header to **ARCHITECTURE MAP** and add a `[+]` icon button to add a new layer/component directly.
- [ ] Expand the `ArchTreeView` to show a proper layer tree (Presentation / Application / Domain / Infrastructure) as collapsible groups, each with their child system/component names as leaf items. Wire selection to highlight the matching card on the canvas.
- [ ] Add a colour dot to each layer group node (purple / green / yellow / blue) matching the card colour scheme.
- [ ] Add a **user profile strip** at the very bottom of the sidebar (avatar initial circle, display name, "Local Workspace" subtitle, settings gear icon).
- [ ] Make the sidebar fixed-width (~220 px) and always visible when a workspace is open — not hidden behind `WorkspaceGrid.IsVisible`.

---

## 3. Architecture Map Canvas — Layered View

**Current:** Canvas cards drawn on `SystemsCanvas`; layer bands are colour-tinted backgrounds toggled by "Sort by Layers". Zoom/canvas-action controls are in a toolbar above the canvas.

**Vision:** The canvas becomes the centrepiece of the **Monitor** tab (Architecture Map):
- "**Architecture Map**" is the panel title; "**Layered View ▾**" is a dropdown in the panel header (next to the title) to switch layout mode.
- Layer labels (**Presentation Layer**, **Application Layer**, **Domain Layer**, **Infrastructure Layer**) are rendered as left-side text inside each horizontal band — in the band's accent colour.
- Cards are visually grouped inside their band with a subtle coloured rounded-rectangle border per layer:
  - Presentation → purple `#9B59B6`
  - Application → green `#27AE60`
  - Domain → gold/amber `#F39C12`
  - Infrastructure → blue `#2980B9`
- Zoom controls (**+**, **−**, fullscreen icon) sit in the **top-right corner of the canvas area** (absolutely positioned), not in the toolbar row above.
- Canvas pan/scroll is smooth; a light grid or no grid background.

### Tasks
- [ ] Add a "**Layered View ▾**" dropdown ComboBox to the `SystemMapView` toolbar (or panel header), replacing/supplementing the current VIEW buttons; its options are: Layered View, Flat View, Compact View.
- [ ] Move the zoom (**−** / `100%` / **+** / Reset) controls and the fullscreen button into an absolutely-positioned overlay in the top-right corner of the canvas `Border`, removing them from the row above the canvas.
- [ ] Render layer-band labels on the **left edge** of each band row as a styled `TextBlock` (rotated 0°, vertically centred in the band), coloured with the layer's accent colour.
- [ ] Update `DrawLayerBands()` / the card-drawing code to apply per-card layer accent colours:
  - Card border stroke = layer colour
  - Card header background = layer colour at ~20% opacity
  - Remove the "Unknown" and confidence-level coloured bars from card faces in Layered View (keep them in System Overview / Flat View).
- [ ] Add a short **description** text line to each card drawn in Layered View (sourced from `SystemItemVm` notes or a new `Description` field).
- [ ] Replace the coloured module-type dot grid on cards with a single line showing a component icon + module count number.

---

## 4. Right Panel — Component / Connections / Properties Tabs

**Current:** Right panel has an ORGANIZE section (four utility buttons) and an Inspector section with Name / Type / Kind / Confidence / Notes / Details fields.

**Vision:** The right panel has three top-level **tabs**: **Component · Connections · Properties**.

### Component tab content (when a component is selected)
- Name (large, bold white)
- `● Active` green badge pill
- `Application Layer` coloured tag badge
- Description paragraph (freeform text)
- **Source** section: filename + `Lines: 45–247`
- **Responsibilities** section: bullet list
- **Log Points (N)** section: list of method names with `Info`/`Warn`/`Error` level coloured badges; `… N more` link
- **Recent Activity** section: a small sparkline / activity chart

### Tasks
- [ ] Add a `TabControl` (Component | Connections | Properties) to the right panel of `SystemMapView.axaml`, replacing the current single-section Inspector.
- [ ] Build the **Component** tab UI: name heading, Active badge, layer tag, description text, Source row, Responsibilities `ItemsControl`, Log Points list with level badges, Recent Activity sparkline placeholder.
- [ ] Wire the Component tab fields from `SystemItemVm` / `RelationshipItemVm` — extend the view-model with `Description`, `SourceFile`, `SourceLineStart/End`, `Responsibilities`, and `LogPointSummaries` properties as needed.
- [ ] Move the **ORGANIZE** buttons (Cleanup Names, Sort Apps from Libraries, Arrange, Sort by Layers) out of the inspector panel and into a small toolbar or context-menu on the canvas, or into a collapsible "Organise" section on the Properties tab.
- [ ] Build the **Connections** tab: list of inbound/outbound relationships for the selected component, each showing direction arrow, target name, and relationship type.
- [ ] Keep (and lightly restyle) the existing **Properties** tab content (Name / Type / Kind / Confidence / Notes / Details).

---

## 5. Bottom Panel — Live Log Stream + Timeline

**Current:** Three tabs (Log Stream / Timeline / Connections) in a 200 px panel. Log Stream shows a plain `ListBox`; Timeline uses `TimelineControl`; Connections shows a `ListBox`.

**Vision:** The bottom area is split **horizontally** into two panes:
- **Left (~65% width):** "Live Log Stream" with a `● Streaming…` indicator and `⏸ Pause` button in the header; a proper data-grid table with columns: **Time · Level · Source · Message**; rows coloured by level (INFO rows have green source text, WARN rows amber); auto-scrolls to latest entry.
- **Right (~35% width):** Two tabs — **Timeline** and **Sessions** — showing a Gantt-style execution chart: method names on the left, coloured horizontal bars on the right showing time ranges, a time ruler at the top, a "Now" red line marker.

### Tasks
- [ ] Replace the bottom `TabControl` with a horizontal `Grid` (two columns, resizable via `GridSplitter`) containing the Log Stream pane on the left and the Timeline/Sessions tab on the right.
- [ ] Style the Log Stream header row: "Live Log Stream" title, a green `● Streaming…` or grey `○ Idle` indicator bound to `LiveMonitorViewModel.IsRunning`, and a `⏸ Pause` / `▶ Resume` toggle button.
- [ ] Replace the plain `ImportedLogsListBox` with a proper columnar table (e.g. `DataGrid` or virtualised `ItemsControl` with column headers): Time | Level | Source | Message. Apply row-level colour coding (green highlight = INFO, amber = WARN, red = ERROR).
- [ ] Remove the replay import/play/pause/stop controls from the Log Stream header toolbar (move Import/Replay controls to the **Devtools** area or a dedicated Replay dialog).
- [ ] Style the Timeline tab to show a Gantt chart: horizontal coloured bars per method call, grouped by source system, with a time axis ruler and a live "Now" indicator line. Wire to `TimelineViewModel` entries.
- [ ] Add a **Sessions** tab next to Timeline showing a session list (date/time start, duration, event count).

---

## 6. Status Bar

**Current:** A single-line `TextBlock` showing generic status text (e.g. "System Map layout updated.").

**Vision:** A rich status bar with segmented info:
- Left: `● Scan Status: Completed` (or `Scanning…` / `Idle`)
- Right: `Files: N,NNN  |  Classes: NNN  |  Methods: N,NNN  |  Log Points: NNN  |  Runtime: HH:MM:SS  |  Events: N,NNN`

### Tasks
- [ ] Expand the status bar `Border` in `MainWindow.axaml` into a `DockPanel` or `Grid` with a left section (scan status) and a right section (metric pills).
- [ ] Expose scan stats (`FileCount`, `ClassCount`, `MethodCount`, `LogPointCount`) on `MainViewModel` (populated after a Roslyn scan).
- [ ] Expose `RuntimeElapsed` (from `LiveMonitorViewModel`) and `TotalEventCount` on `MainViewModel` and bind them to the right status section.
- [ ] Style the scan-status dot: green = Completed, amber = In Progress, grey = Idle.

---

## 7. Card Design Refinement (Layered View)

**Current cards show:** name bar, Unknown/Kind coloured bar, confidence bar, module count text, a row of small coloured module-type dots, "log scale" label, Other×N / Cfg×N counts, Console Application startup note.

**Vision cards show (in Layered View):** clean rounded card, layer-coloured border/header, large name, 1–2 line description, and a single `⚙ N` module count badge. No confidence bars, no dot grid.

### Tasks
- [ ] Create a distinct card-drawing path in `SystemMapView.axaml.cs` for **Layered View mode** that renders the simplified card layout (name + description + module count icon).
- [ ] Keep the existing detailed card drawing for **Flat / System Overview** mode (confidence bars, dot grid, etc.) so no existing functionality is lost.
- [ ] Ensure card width and height are consistent within a layer row and that the minimum card width accommodates the description line.
- [ ] Add `CornerRadius` = 8 px to all card borders in Layered View for the rounder look visible in the vision.

---

## 8. General Polish

- [ ] Adopt the `codomon` (all lowercase) branding consistently: window title, welcome screen, About dialog.
- [ ] Update `App.axaml` colour tokens to add layer accent colours as named resources (`LayerPresentationColor`, `LayerApplicationColor`, `LayerDomainColor`, `LayerInfrastructureColor`).
- [ ] Add smooth-scroll / animated pan support to the canvas zoom control so zooming/panning feels fluid.
- [ ] Review all button labels and toolbar text for tone consistency with the vision (e.g., "Scan" not "Scan…", "Stop" not "⏹ Stop").
- [ ] Ensure the welcome/recent-workspaces overlay uses the same `codomon` lowercase wordmark.
